'use strict';

const tem1 = Number(prompt('Enter tem1:'));
const tem2 = Number(prompt('Enter tem2:'));
const tem3 = Number(prompt('Enter tem3:'));

let hTem = 0;

if (tem1 <= tem2) {
    hTem = tem1;
} else if {
    
}